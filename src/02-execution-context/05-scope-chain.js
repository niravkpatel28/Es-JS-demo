const firstName = "Nirav";

const printAddress = () => {
  let address = "Some street";
  console.log("Some value", lastName);
};

const printSomething = (address = "something") => {
  const lastName = "Patel";
  console.log(firstName + " " + lastName);

  // firstName pritnAddress, address, lastName ---
};

printSomething();
// EC1
// Variable Object : { variable, functions }-- defines your scope chain

// Scope chain  - List of variables and functions which my current function can access
